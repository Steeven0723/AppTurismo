import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.ProTourism',
  appName: 'ProTourism',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
